@model BasicWMS.Models.Purchase

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Purchase</legend>

        @Html.HiddenFor(model => model.PurchaseId)

        <div class="editor-label">
            @Html.LabelFor(model => model.NumberReceived)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.NumberReceived)
            @Html.ValidationMessageFor(model => model.NumberReceived)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.PurchaseDate)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.PurchaseDate)
            @Html.ValidationMessageFor(model => model.PurchaseDate)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
